spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/ledger
    username: ledger
    password: ledger
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate.jdbc.time_zone: UTC
  data:
    redis:
      host: localhost
      port: 6379